import React, { FC } from 'react';
import { Container } from './Main.Styled';
import Header from '../Header/Header';

const Main: FC = () => (
  <Container>
    <Header></Header>
  </Container>
);

export default Main;
